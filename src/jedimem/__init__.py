"""jedimem — git-native team memory for coding agents.

Standard library only, by design: the install path must not require a package
manager, a virtualenv, or network access.
"""
__version__ = "0.1.0"
FORMAT_VERSION = 1
